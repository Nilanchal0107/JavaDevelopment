public class Demo {
    public static void main (String A[]) {
        final int MAX = 100;
        System.out.println(MAX);

        // MAX = 99;
        // When we use final keyword before a variable, tha variable becomes constant and can't be changed it.

    }
}